{
  "name": "fuel-session-policy-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript-first Fuel Session Policy SDK monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "setup:local": "node scripts/setup-local.mjs",
    "dev": "npm run dev --workspace @fuel-session-policy/demo-web",
    "build": "npm run build --workspace @fuel-session-policy/shared && npm run build --workspace @fuel-session-policy/sdk && npm run build --workspace @fuel-session-policy/demo-web",
    "test": "npm run test --workspaces",
    "fuel:build": "node scripts/build-predicate.mjs"
  }
}
