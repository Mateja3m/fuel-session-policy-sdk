# Shared Workspace (Private)

This folder contains internal shared types/constants for local workspace use.

- It is not part of MVP public API guarantees.
- Keep this package minimal and avoid leaking accidental surface area.
